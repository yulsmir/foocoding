'use strict';
export class User {
  constructor(id, firstName, lastName, email, gender) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.gender = gender;
  }
}
